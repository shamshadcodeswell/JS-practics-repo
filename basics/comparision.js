let c = 2>3; // prints true/false
// console.log(c);

console.log(null == 0); // null is hardcoded such that it doesnt corecs null AND NUL == UNDEFINED returns true

// whereas >= coerces null to 0
console.log(undefined >= 0);
